# Service Health Checker (Bash)

A simple Bash script to monitor system services and log their status.

## Features
- Checks services listed in `services.conf`
- Logs results with timestamps
- Displays live output
- Easy to extend with alerts

## Setup
1. Add service names to `services.conf` (one per line)
